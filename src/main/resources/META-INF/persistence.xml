<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
               https://jakarta.ee/xml/ns/persistence
               https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <!--
        persistence-unit name phải khớp với chuỗi trong JpaUtil.PERSISTENCE_UNIT = "ktxPU"
    -->
    <persistence-unit name="ktxPU" transaction-type="RESOURCE_LOCAL">

        <!-- JPA Provider: Hibernate -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Khai báo toàn bộ Entity class -->
        <class>com.ktx.model.NhanVien</class>
        <class>com.ktx.model.SinhVien</class>
        <class>com.ktx.model.DayNha</class>
        <class>com.ktx.model.Phong</class>
        <class>com.ktx.model.DonDangKy</class>
        <class>com.ktx.model.HopDong</class>
        <class>com.ktx.model.CauHinhGia</class>
        <class>com.ktx.model.HoaDon</class>

        <!-- Không quét tự động các class Entity ngoài danh sách trên -->
        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <!-- ====================================================
                 Kết nối MySQL – chỉnh sửa PASSWORD cho phù hợp
                 ==================================================== -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mysql://localhost:3306/QuanLyKTX?useUnicode=true&amp;characterEncoding=UTF-8&amp;serverTimezone=Asia/Ho_Chi_Minh&amp;useSSL=false&amp;allowPublicKeyRetrieval=true"/>
            <property name="jakarta.persistence.jdbc.user"     value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="123456"/>

            <!-- ====================================================
                 Hibernate dialect & logging
                 ==================================================== -->
            <property name="hibernate.dialect"
                      value="org.hibernate.dialect.MySQLDialect"/>

            <!-- In SQL ra console khi debug (đổi thành false khi release) -->
            <property name="hibernate.show_sql"   value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- none: bỏ qua validation schema – dùng khi DB có cột ENUM -->
            <property name="hibernate.hbm2ddl.auto" value="none"/>

            <!-- Connection Pool tối thiểu cho ứng dụng desktop -->
            <property name="hibernate.connection.pool_size" value="5"/>
        </properties>

    </persistence-unit>
</persistence>
